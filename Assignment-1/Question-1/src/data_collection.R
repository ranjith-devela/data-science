#Copied the question data into csv
#1. Data Collection - Importing raw data
strength_raw_data <- read.csv("~/strength_raw_data.csv", stringAsFactor= T)

#Viewing the raw data
View(strength_raw_data)